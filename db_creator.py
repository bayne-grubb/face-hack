import sqlite3
from sqlite3 import Error
import cv2
import face_recognition


print(male_face_encoding)
"""try:
    conn = sqlite3.connect()
    print(sqlite3.version)
    conn.cursor().execute("CREATE TABLE IF NOT EXISTS faces ( name text ")
except Error as e:
    print(e)
finally:
    conn.close()
"""
